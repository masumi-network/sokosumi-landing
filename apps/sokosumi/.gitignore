# generated catalog cache (regenerated by server.js on boot + every refresh)
data/
# local preview-tool screenshots
.preview/
# large source hero video — kept out of git (compress / Git LFS before launch)
assets/hero-bg.mp4
