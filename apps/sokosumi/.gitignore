# Next.js
/.next/
/out/
next-env.d.ts

# deps
/node_modules

# env (never commit the Core API key)
.env*.local

# leftover static-placeholder artifacts (superseded by the Next app)
/.preview/
/data/
/assets/hero-bg.mp4
